package ru.netology.nework.entity

import androidx.room.Entity
import ru.netology.nework.enumeration.UserListType

/*
@Entity(primaryKeys = ["noteTypeCode", "noteEntityId", "userListType", "userId"])
data class NoteUserListsEntity(
    val noteTypeCode: Int,
    val noteEntityId: Long,
    val userListType: String, //UserListType - TODO - сделать числовое поле userListTypeCode
    val userId: Long,
    )
*/
